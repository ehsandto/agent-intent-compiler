# Architecture

`Authenticated intent owner -> accepted independent planner -> reserved immutable plan version -> source/hash verification -> deterministic DAG validation -> independent semantic compilation -> exact report consensus -> fingerprint-bound execution gate`.
