import fs from"node:fs";import assert from"node:assert/strict";const s=fs.readFileSync("contracts/AgentIntentCompiler.py","utf8");assert.match(s,/leader==validator/);assert.match(s,/planner_accepted/);assert.match(s,/version_reserved/);assert.match(s,/activation head changed/);assert.match(s,/source_fingerprints/);assert.match(s,/hash_matches/);assert.match(s,/UNAVAILABLE/);assert.match(s,/REQUIRES_REVISION/);assert.match(s,/verify_execution_ready/);assert.equal(fs.readdirSync("contracts").filter(x=>x.endsWith(".py")).length,1);console.log("10 invariant checks passed")
